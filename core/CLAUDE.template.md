# Project Brain

> Generated by `scripts/apply.sh` from the Claude Boilerplate. The Stack, Commands, Project layout, and Anti-patterns sections come from the base overlay's fragment. Integration paragraphs are appended at the bottom.

## Quick orientation

This file is the **high-level map** for the project. Detailed rules live in `.claude/rules/`. The pre-commit hook (`.claude/hooks/pre-commit-validate.sh`) gates every `git commit` with ESLint → Prettier → `/security-review` → `/code-review` — assume your changes have to clear all four. This file is read once at session start; use `/clear` and `/compact` to manage context within a session.

## Project context

> **Fill these in for your project.** They are placeholders Claude relies on for project-specific decisions.
>
> - **Mandatory** sections should be filled before the first commit — they shape every other decision.
> - **Recommended** sections add high-value context.
> - **Optional** sections are for teams that want them. Delete what you don't use.

### Principles · *Recommended*

> *Guiding principles that shape technical decisions. Example: "We prefer simple over clever." · "API stability is a feature." · "Reads before writes." · "Boring tech wins."*

_TODO: list 3–7 principles, one bullet each._

### Architecture · *Mandatory*

> *The system-level view. What services / processes exist? How do they talk? What's the data flow? Link a diagram if you have one. Different from "Project layout" below (which is files); this is "what runs and how it talks."*

_TODO: 1 paragraph + (optionally) a Mermaid or ASCII diagram._

### Data Models · *Mandatory for projects with persistent data*

> *Core entities, relationships, and key fields. Don't duplicate the database schema — describe the conceptual model that ties to the domain.*

_TODO: list each entity with a one-line role + the key relationships._

### API · *Mandatory for projects exposing an API*

> *The surface area: what does this service expose, to whom, with what auth? Endpoint-level contracts live in OpenAPI / Swagger — this is the map.*

_TODO: list the resource groups (e.g. `/auth/*`, `/users/*`, `/billing/*`) with one line each._

### Auth · *Mandatory if the project has authentication*

> *Identity provider, session model, role model, what claims to check on protected routes. Security-critical — without it Claude has to re-derive your auth model every session.*

_TODO: identity provider, session strategy, role hierarchy, where the rules are enforced._

### NFRs · *Recommended*

> *Non-functional requirements. Performance budgets (p95 latency, RPS), scalability targets, availability target, security posture (SOC 2 / GDPR / PCI / HIPAA), data residency.*

_TODO: list 3–6 numeric or categorical NFRs._

### Phase 1 Scope · *Optional*

> *What's in and out of the first version. Useful for new projects in their first 0–6 months. Delete this section once v1 has shipped.*

_TODO: "In scope" bullets + "Out of scope (deferred)" bullets._

### Workflows · *Recommended*

> *Project-specific workflows beyond the standard "Common workflows" below. Examples: "How we ship to staging" · "How we run a data migration" · "How we onboard a new tenant" · "How we rotate secrets."*

_TODO: one sub-section per workflow with the exact steps._

### Project Structure · *Recommended if you deviate from the base default*

> *Project-specific layout additions beyond what the base ships. The base already documents the standard layout — only fill this when you've added something non-standard.*

_TODO: list the directories you added/renamed and why._

### Glossary · *Recommended*

> *Project-specific terminology. Saves new joiners (and Claude) from getting the domain model wrong. Example: "A workspace is a tenant." · "A job is a background task, not a CI run."*

_TODO: 5–15 terms with one-line definitions._

### External Dependencies · *Recommended*

> *Third-party services this project depends on (Stripe, Sendgrid, Segment, OpenAI, S3, Datadog, etc.). Each one is a coupling point.*

_TODO: name · purpose · failure mode (what happens when it's down)._

### Environments · *Recommended*

> *Dev / staging / production URLs and the differences that matter (different DB, different feature flags, different rate limits).*

_TODO: table of `environment | URL | DB | notes`._

### Deployment · *Mandatory*

> *How code ships. CI/CD pipeline? Vercel? Docker? Manual? What triggers a deploy? What's the rollback procedure?*

_TODO: deploy trigger · pipeline · rollback steps._

### Observability · *Optional*

> *Logs, metrics, traces — where to look when things break. Skip if you don't have an observability stack yet.*

_TODO: log destination · metrics dashboard · alert routing._

### Tasks · *Optional*

> *Current in-flight work. Most teams keep this in Jira / Linear / Asana — skip this section if so. Useful for solo devs or very small teams.*

_TODO: a short list of what's in progress, or a link to your task tracker._

### Severity · *Optional*

> *Incident severity definitions and response times. Useful for on-call orgs.*

_TODO: SEV1 / SEV2 / SEV3 definitions + response SLAs._

## Stack

{{STACK}}

## Commands

{{COMMANDS}}

## Project layout

{{ARCHITECTURE}}

## Conventions

### Code style

- `const` by default, `let` when mutation is required, never `var`.
- `async`/`await` over raw promises.
- Naming: `camelCase` variables, `PascalCase` classes/types, `SCREAMING_SNAKE` constants.
- Max function length: 30 lines. Max file length: 300 lines.
- Prefer pure functions and explicit dependencies. Side-effects live in clearly-named modules.

### Error handling — see `.claude/rules/error-handling.md`

- Always wrap async code in try/catch (or framework equivalents like `express-async-errors`).
- Meaningful messages; never swallow errors silently.

### Testing — see `.claude/rules/testing.md`

- Cover happy path + edge cases + error paths + security paths.
- Coverage target ≥ 80%. Tests live alongside source (`foo.js` → `foo.test.js`).

### Security — see `.claude/rules/security.md`

- Validate every external input. Parameterised queries only — no SQL string concatenation.
- No hardcoded secrets. Env vars only — see `.env.example`.

### Environment variables — see `.claude/rules/environment.md` + `.env.example`

- Public keys get the framework prefix (`NEXT_PUBLIC_`, `VITE_`); secrets never do.
- `.env` is gitignored. `.env.example` lists every required key with safe placeholders.

## Git

- Commits: conventional (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`).
- The pre-commit hook handles lint, format, security, and code review automatically. Don't squash these gates around — they protect the branch.

### Branch naming

| Change type | Branch pattern |
|---|---|
| New feature | `feature/<ticket-or-slug>` |
| Bug fix | `bugfix/<ticket-or-slug>` |
| Production hotfix | `hotfix/<ticket-or-slug>` |
| Chore / housekeeping | `chore/<ticket-or-slug>` |
| Docs only | `docs/<ticket-or-slug>` |
| Refactor (no behavior change) | `refactor/<ticket-or-slug>` |
| Tests only | `test/<ticket-or-slug>` |

Rules:
- Use the ticket ID if present: `feature/PROJ-123`.
- Otherwise a short slug (2–4 words, lowercase, hyphens): `bugfix/auth-token-expiry`.
- Never use pipeline-tier prefixes (`nano/`, `task/`, `standard/`) — those are workflow internals, not branch names.

## Pre-commit pipeline

> **The commit rule:** code is not committed until the pre-commit gates pass. The 4-step pipeline below is non-negotiable. If you adopt stage-based agents (Orchestrator / CEO / Eng / Design / Build / Testing / Review / Ship), only the **Review-stage agent** issues `git commit` — all other agents hand off. This is enforced at three levels: (1) the pre-commit hook, (2) agent definitions (only Review outputs git commands), (3) any orchestrator rejects `git commit`/`push` from non-Review agents.

Every `git commit` runs `.claude/hooks/pre-commit-validate.sh`:

| Step | Gate | Blocks on |
|---|---|---|
| 1 | ESLint | lint errors |
| 2 | Prettier | formatting drift |
| 3 | `/security-review` | injection, XSS, hardcoded secrets, path traversal |
| 4 | `/code-review` | "Request Changes" verdict or critical findings |

Reports → `.reviews/<sha>.md`.

If `security-guidance` or `code-review` plugin is missing, that gate **skips with a warning** instead of blocking. Set `CLAUDE_BOILERPLATE_STRICT=1` to fail hard instead. Install both with:

```
claude /plugins install https://claude.com/plugins/security-guidance
claude /plugins install https://claude.com/plugins/code-review
```

Verify the whole environment any time with `npm run check-setup`.

## Rule files

These rules live in `.claude/rules/` and Claude reads them as the situation calls for one:

**Core (always present)**
- `error-handling.md` — try/catch patterns, error envelopes
- `security.md` — input validation, secrets, dangerous APIs
- `environment.md` — env-var conventions
- `testing.md` — test buckets, coverage discipline

**Base-specific** (from this project's stack — see the layout above)

**Integration-specific** (from each integration applied — see the Integration sections at the bottom)

**Project-specific** (yours — put them in `.claude/rules/project/`)

## Docs convention

If your team uses staged docs per task, follow this naming so authorship and audience are unambiguous. Skip if you don't have a staged process — the pattern works once you do.

| File | Written by | Read by |
|---|---|---|
| `docs/spec-<id>.md` | Eng / planning | Build, Testing, Review |
| `docs/design-<id>.md` | Design | Build, Testing, Review |
| `docs/progress.md` | Implementer | Testing, Review |
| `docs/review-<id>.md` | Review gate | Ship, audit trail |
| `CHANGELOG.md` | Release | everyone |

`<id>` is the ticket ID or branch slug — same identifier used in the branch name (`feature/PROJ-123` → `docs/spec-PROJ-123.md`).

## Common workflows

### Start any task

`/start <description>` is the universal entry point — feature, bug fix, hotfix, refactor, chore, docs. The **classifier** (`.claude/agents/classifier.md`) sizes the task into `nano | standard | full` and picks the branch name. The **orchestrator** (`.claude/workflows/orchestrator.md`) loads the matching pipeline.

Examples:
- `/start add Stripe billing to checkout`
- `/start fix the auth token expiry crash on refresh`
- `/start hotfix the null-pointer in payment-service`
- `/start refactor user service into smaller modules`

Each stage stops at its gate. Use `/approve` to advance, `/status` to inspect, `/review` for an on-demand review, `/ship` to merge and tag (Review approval required first).

### Update dependencies

1. `npm run deps:audit && npm run deps:outdated`.
2. Treat dependency updates like any other task: `/start update outdated dependencies` and let the classifier route through the pipeline.

## Anti-patterns ("Looks right but is wrong")

{{ANTIPATTERNS}}

## Rules for Claude

- ✅ Follow existing patterns in the codebase before inventing new ones.
- ✅ Write tests for new functions (happy + edge + error).
- ✅ Use existing utilities before creating new ones.
- ✅ Follow rules in `.claude/rules/` for domain-specific guidance.
- ❌ Don't introduce new dependencies without discussion.
- ❌ Don't modify `.claude/hooks/` without approval — they fire on every commit.
- ❌ Don't skip error handling.
- ❌ Don't hardcode secrets — use environment variables.

## Customizing this file

This file was generated by the Claude Boilerplate. To extend safely without making upgrades painful:

- **Add a project-specific rule:** drop it in `.claude/rules/project/<name>.md`. The boilerplate's upgrade machinery never touches anything under `project/`.
- **Override a managed rule:** create `.claude/rules/<name>.override.md` next to the managed file. Claude reads both; the override wins on conflicts. Survives upgrades.
- **Extend the pre-commit pipeline:** drop a script in `.claude/hooks/pre-commit.d/` named `before-NN-<name>.sh` (runs before step 1) or `after-NN-<name>.sh` (runs after step 4). Sort order by numeric prefix.
- **Add a project workflow:** put it in `.claude/workflows/project/`. Workflows under `project/` are user-namespaced — upgrades won't touch them.

For the full guide see `docs/customizing.html` in the boilerplate repo.

## Token tips

- Run `/compact` after completing a logical sub-task.
- Use `/clear` between unrelated tasks.
- Reference specific files/lines in prompts, not "look at the codebase."
- Cap long CLI output: `npm test 2>&1 | head -50`.

---

*Assembled by [Claude Boilerplate](https://github.com/your-org/claude-js-boilerplate) `apply.sh`. Re-running `apply.sh upgrade` warns before overwriting any local edits.*
