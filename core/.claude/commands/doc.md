Add or update JSDoc documentation for $ARGUMENTS.

## Instructions
1. Read the target file
2. For every exported function, class, and constant, add/update JSDoc comments:
   - `@description` — What it does
   - `@param {type} name` — Each parameter with type and description
   - `@returns {type}` — Return value description
   - `@throws {Error}` — If the function can throw
   - `@example` — A usage example
3. Do NOT modify any logic — only add/update documentation comments
4. Keep descriptions concise (one line where possible)
5. Match the existing code style (single quotes, 2-space indent)
