Explain the file or module at $ARGUMENTS in detail:

1. **Purpose** — What does it do and why does it exist?
2. **Key data flow** — Trace the main input → processing → output path
3. **Dependencies** — What does it import/require and what depends on it?
4. **Side-effects** — File I/O, network calls, global state mutations
5. **Edge cases** — Known gotchas, error scenarios, boundary conditions

If the flow is complex, include a mermaid diagram.
If $ARGUMENTS is a directory, provide a high-level overview of all files in it.
