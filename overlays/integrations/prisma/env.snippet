# Prisma — pooled connection used by the app at runtime
DATABASE_URL=postgresql://user:pass@host:5432/db?schema=public
# Direct connection for migrations (required when DATABASE_URL goes through a pooler)
DIRECT_URL=postgresql://user:pass@host:5432/db?schema=public
